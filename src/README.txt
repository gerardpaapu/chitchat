runtime
-------

Provides the chitchat runtime as a set of node.js modules, browserify compatible


compiler
--------

compile chitchat source to javascript (using a very simple/dumb compiler w/ a lot of keywords)


compiler2
----------

Sketch of a compiler with a compile-time environment including macros, primitives and very few keywords

Should really be in a branch rather than a folder
